[build]
target = "index.html"
dist = "dist"

[serve]
port = 8080
open = false
proxy_backend = "http://localhost:3000"

[[proxy]]
rewrite = "/api"
backend = "http://localhost:3000/api"

[[proxy]]
rewrite = "/ws"
backend = "ws://localhost:3000/ws"
